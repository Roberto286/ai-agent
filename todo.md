# NEW TODO

1.  **`search_files(pattern: str, directory: str = None, file_extension: str = None)`**: This function would allow me to search for specific patterns or strings within files, optionally filtering by directory a
    nd file extension. This would greatly improve my ability to quickly locate relevant code, definitions, or usages across a codebase, which is crucial for understanding and modifying projects.

2.  **`delete_path(path: str)`**: This function would enable me to delete files or empty directories. This is essential for cleanup, refactoring, and managing temporary files, providing more complete control ov
    er the file system. I would, of course, exercise caution and seek user confirmation before using such a function.

3.  **`create_directory(path: str)`**: This function would allow me to explicitly create new directories. This is useful for organizing files into new structures and setting up new modules or components.

4.  **`move_path(source: str, destination: str)`**: This function would enable me to rename files or directories, or move them to a different location. This is a common operation during refactoring and project
    organization.

5.  **`get_diff(file_path_1: str, file_path_2: str = None, content_1: str = None, content_2: str = None)`**: This function would generate a diff between two files or between two provided content strings. This w
    ould be invaluable for verifying changes before writing them to a file, helping to prevent unintended modifications and improving the accuracy of my code changes.
